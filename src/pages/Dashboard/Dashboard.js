import React from "react";

const Dashboard = () => {
  //props: {name: string}
  return (
    <div>
      <h1>Dashboard</h1>
      {/* {props.name ? `Hi ${props.name}`:`You are not Authenticated!`} */}
    </div>
  );
};

export default Dashboard;
